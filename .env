# Host ports for observability services (high range avoids clashes on shared servers).
# Override in your shell or edit this file if a port is still taken on your machine.
MLFLOW_PORT=25000
OPTUNA_PORT=28080
STREAMLIT_PORT=28501
MLFLOW_TRACKING_URI=http://localhost:25000
