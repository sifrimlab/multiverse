# PCA: CPU stack; pytorch/scanpy from conda-forge, versions aligned with project pins.
name: multiverse_pca
channels:
  - conda-forge
dependencies:
  - pip
  - ipykernel
  - python=3.11
  - pytorch
  - scanpy==1.10.3
  - pip:
      - muon==0.1.7
      - jaxlib==0.4.34
      - louvain==0.8.2
